"""Component editor — author/align CircuiTikZ symbols (spec/component-editor.md).

`renderer` is the Qt-free render/save core (shared with tools/generate_components.py);
`draft` is the editing model; `window` is the standalone Qt GUI
(``python -m app.componenteditor``).
"""
